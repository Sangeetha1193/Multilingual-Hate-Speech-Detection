# Multilingual-Hate-Speech-Detection
An end-to-end NLP pipeline that identifies hate speech in 5 languages using mBERT. Includes data collection, augmentation, model training, and deployment via a Flask API with a real-time browser extension for live detection.
